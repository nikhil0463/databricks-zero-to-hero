bundle:
  name: DABDemo

include:
  - src/*.yml
  - src/env/*.yml
  - resources/jobs/*.yml
  - resources/pipelines/*.yml
  - resources/schemas/*.yml

permissions:
  - user_name: sainikhilde@gmail.com
    level: CAN_MANAGE
  - group_name: users
    level: CAN_MANAGE

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-371dc522-2690.cloud.databricks.com

  qa:
    default: false
    workspace:
      host: https://dbc-371dc522-2690.cloud.databricks.com
